import 'package:flutter/material.dart';

class Shedule extends StatelessWidget {
  const Shedule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('route Content')));
  }
}
